package blockc; import java.util.Date; import java.security.MessageDigest; public class Block public String hash, previ- ousHash; private String data; private long timestamp; String blockData; public Block(String data, String previousHash) this.data = data; this.previousHash = previousHash; this.timestamp = new Date().getTime(); this.hash = calculateHash(); blockData = this.data + " " + this.previousHash + " " + this.timestamp + " " + this.hash; System.out.println("Contents of Block are " + blockData); // Calculating new hash based on blocks contents public String calculateHash() String calculatedHash = Block.md5(previousHash + Long.toString(timestamp) + data); return calculatedHash; public static String md5(String input) String digestString = null; try MessageDigest md = MessageDigest.getInstance("MD5"); byte[] ba = input.getBytes("UTF8"); md.update(ba); byte[] digest = md.digest(); System.out.println("Result: Success"); digestString = digest.toString(); catch (Exception e) System.out.println(e); return digestString;